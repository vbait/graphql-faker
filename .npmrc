# graphql is on 17 while graphql-http declares `peer graphql "<=16"`.
# The combo is verified working; this lets `npm i` succeed without
# `--legacy-peer-deps` on the CLI (the peer conflict becomes a warning).
legacy-peer-deps=true
